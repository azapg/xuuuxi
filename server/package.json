{
  "name": "@xuuuxi/server",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "echo 'No server build needed, running from source'",
    "start": "bun src/index.ts",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "bun src/db/seed.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@xuuuxi/shared": "workspace:*",
    "drizzle-orm": "^0.44",
    "zod": "^3.25"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31"
  }
}
