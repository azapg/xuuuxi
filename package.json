{
  "name": "xuuuxi",
  "private": true,
  "workspaces": ["shared", "server", "client"],
  "engines": {
    "node": ">=20",
    "bun": ">=1"
  },
  "scripts": {
    "dev": "concurrently \"bun run --cwd server dev\" \"bun run --cwd client dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
